"use strict";
// Pure Functions: Pure functions always produce the same output for the same input and have no side effects.
function add(a, b) {
    return a + b; // Always returns the same output for the same inputs
}
// Usage
const result = add(5, 10); // result is 15
//# sourceMappingURL=pure.js.map