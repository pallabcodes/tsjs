enum Status {
  Pending,
  FullFilled,
  Success,
  Rejected,
  NetworkError
}

// # string enums
enum Language {
  React = `React`,
  Angular = `Angular`,
  Node = `Node`,
  Next = `Next`,
  Nest = `Nest`,
}
