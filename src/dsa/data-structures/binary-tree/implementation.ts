import Comparator from "../../../utils/comparator";

export default class BinaryTreeNode {
    parent = ;

    constructor(value = null) {
        this.parent = null;

    }
}

function Game () {
    this.title = 'golang'
}